# R12 Overall 0.85 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a train-only, 8GB-compatible R12 candidate stack that improves short-command recognition and target-speaker gating toward Overall 0.85.

**Architecture:** Keep the existing immutable Dataset-A split and add explicit ASR candidate controls, a small enrollment-conditioned Personal VAD, and label-free router features. Preserve the existing calibrated gate contract and publish no private data or model outputs.

**Tech Stack:** Python 3.11, pytest, NumPy, soundfile, PyTorch when installed, FunASR when installed, scikit-learn, existing R12 JSONL contracts.

**Spec:** `docs/superpowers/specs/2026-08-27-r12-overall-085-design.md`

## Global Constraints

- Work only in `F:\XH-202615\XH-202615\.worktrees\r12-dataa-augmented-internal` on `codex/r12-dataa-augmented-internal`.
- Preserve untracked `.arena/` and all existing user changes.
- Never read internal-test labels in training, augmentation, hotword construction, model selection, or threshold selection.
- Augmented rows inherit their parent group and remain train-only.
- Model artifacts, Dataset-A audio, labels, caches, and outputs remain outside the committed repository surface.
- Do not require FunASR or a GPU for unit tests and dry-run tests.
- Every production behavior is introduced by a failing test first.

### Task 1: Add explicit 8GB LoRA training recipe fields

**Files:**
- Modify: `xh202615/r12_asr_train.py`
- Modify: `configs/r12_paraformer_train.example.yaml`
- Test: `tests/test_r12_asr_train.py`

**Interfaces:**
- Extend `TrainingConfig` with `lora_rank: int = 8`, `lora_alpha: int = 16`, `lora_dropout: float = 0.05`, `learning_rate: float = 1e-4`, `max_epoch: int = 30`, `keep_nbest_models: int = 10`, `avg_nbest_model: int = 5`, `batch_size: int = 800`, `accum_grad: int = 8`, and `num_workers: int = 2`.
- `build_train_argv` emits encoder and decoder LoRA settings and the training/resource overrides without importing FunASR.

- [ ] **Step 1: Write failing tests**

Add tests that build a default config and assert the argv includes `encoder_conf.lora_list=[q,k,v,o]`, rank/alpha/dropout for both encoder and decoder, `optim_conf.lr=0.0001`, `train_conf.max_epoch=30`, `train_conf.keep_nbest_models=10`, `train_conf.avg_nbest_model=5`, `dataset_conf.batch_size=800`, `dataset_conf.num_workers=2`, and `accum_grad=8`. Add invalid-value tests for rank, dropout, learning rate, and accumulation.

- [ ] **Step 2: Run RED**

Run: `F:\XH-202615\XH-202615\.venv\Scripts\python.exe -m pytest tests\test_r12_asr_train.py -q`

Expected: FAIL because the new fields and argv entries do not exist.

- [ ] **Step 3: Implement minimal recipe serialization**

Validate positive integer fields, dropout in `[0,1)`, and finite positive learning rate. Keep current internal-test manifest rejection. Emit exact Hydra overrides only for `lora` mode; retain `freeze_encoder` behavior unchanged.

- [ ] **Step 4: Run GREEN**

Run: `F:\XH-202615\XH-202615\.venv\Scripts\python.exe -m pytest tests\test_r12_asr_train.py -q`

Expected: PASS.

- [ ] **Step 5: Commit**

Run: `git add xh202615/r12_asr_train.py configs/r12_paraformer_train.example.yaml tests/test_r12_asr_train.py; git commit -m "feat: expose bounded Paraformer LoRA recipe"`

### Task 2: Add no-VAD, padding, and train-only subphrase hotword contracts

**Files:**
- Modify: `scripts/run_funasr_asr.py`
- Modify: `xh202615/r12_asr_hotword.py`
- Create: `scripts/r12_asr_prepare_subphrase_hotwords.py`
- Test: `tests/test_r12_asr_hotword.py`
- Test: `tests/test_r12_asr_smoke.py`

**Interfaces:**
- Add `prepare_subphrase_candidates(train_labels_path, train_parent_ids, output_root, capacities=(10,20,40)) -> HotwordSummary`.
- The private hotword JSON maps capacity strings to space-joined normalized subphrases; the public summary contains only counts and digests.
- `run_funasr_asr.py` accepts `--vad-model off`, `--punc-model off`, and `--pad-ms`; `off` becomes an omitted model argument and padding is applied in-memory before inference.

- [ ] **Step 1: Write failing tests**

Test that repeated 2–4 character subphrases rank by frequency and UTF-8 order, null labels are excluded, capacities over the ranked list fail closed, and public summaries contain no phrase text. Test CLI parser acceptance of `off` and `pad-ms`, and test the pad helper adds equal zero samples on both sides at 16kHz.

- [ ] **Step 2: Run RED**

Run: `F:\XH-202615\XH-202615\.venv\Scripts\python.exe -m pytest tests\test_r12_asr_hotword.py tests\test_r12_asr_smoke.py -q`

Expected: FAIL because subphrase preparation and padding controls are absent.

- [ ] **Step 3: Implement minimal helpers**

Normalize labels with the existing ASR target normalizer, generate contiguous Chinese/ASCII n-grams of lengths 2–4, count only train parent labels, and preserve existing whole-command builder behavior. Add a pure `pad_audio(audio, rate, pad_ms)` helper and pass empty model settings to FunASR when `off` is selected.

- [ ] **Step 4: Run GREEN**

Run: `F:\XH-202615\XH-202615\.venv\Scripts\python.exe -m pytest tests\test_r12_asr_hotword.py tests\test_r12_asr_smoke.py -q`

Expected: PASS.

- [ ] **Step 5: Commit**

Run: `git add scripts/run_funasr_asr.py xh202615/r12_asr_hotword.py scripts/r12_asr_prepare_subphrase_hotwords.py tests/test_r12_asr_hotword.py tests/test_r12_asr_smoke.py; git commit -m "feat: add short-command ASR candidates"`

### Task 3: Add CPU-friendly Personal VAD model and train-only mixture contracts

**Files:**
- Create: `xh202615/r12_personal_vad.py`
- Create: `scripts/r12_prepare_personal_vad_mixtures.py`
- Create: `scripts/r12_train_personal_vad.py`
- Create: `scripts/r12_infer_personal_vad.py`
- Test: `tests/test_r12_personal_vad.py`

**Interfaces:**
- `PersonalVADConfig(mel_bins=80, hidden_size=128, num_layers=2, sample_rate=16000)`.
- `build_personal_vad_features(log_mel, enrollment_embedding, frame_cosine) -> np.ndarray` validates aligned finite arrays.
- `aggregate_personal_vad(frame_probabilities, frame_seconds=0.01) -> dict[str, float]` emits fixed label-free gate features.
- `PersonalVADNet(config)` consumes `[batch, frames, feature_dim]` and returns `[batch, frames, 3]` logits.
- Mixture preparation accepts train-only speaker-indexed source manifests and writes a lineage manifest that records parent IDs, source speaker IDs, and mixture parameters without internal-test paths.

- [ ] **Step 1: Write failing tests**

Test feature alignment and finite checks, three-class output shape, deterministic aggregation including target ratio and longest run, and mixture lineage rejection of any path containing `internal_test` or `held_out`. Add a CPU forward test with a tiny tensor.

- [ ] **Step 2: Run RED**

Run: `F:\XH-202615\XH-202615\.venv\Scripts\python.exe -m pytest tests\test_r12_personal_vad.py -q`

Expected: FAIL because the module and contracts are absent.

- [ ] **Step 3: Implement minimal model and contracts**

Use a GRU with `batch_first=True` and a linear three-class head. Compute stable softmax probabilities in the inference helper, reject non-finite input, and calculate target class ratio, max target probability, longest contiguous target run, and overlap probability. Keep training CLI optional when PyTorch is unavailable; unit tests remain CPU-only.

- [ ] **Step 4: Run GREEN**

Run: `F:\XH-202615\XH-202615\.venv\Scripts\python.exe -m pytest tests\test_r12_personal_vad.py -q`

Expected: PASS.

- [ ] **Step 5: Commit**

Run: `git add xh202615/r12_personal_vad.py scripts/r12_prepare_personal_vad_mixtures.py scripts/r12_train_personal_vad.py scripts/r12_infer_personal_vad.py tests/test_r12_personal_vad.py; git commit -m "feat: add enrollment-conditioned Personal VAD"`

### Task 4: Extend router features and canonical integration

**Files:**
- Modify: `xh202615/r12_candidate_router.py`
- Modify: `xh202615/r12_calibrated_gate.py`
- Modify: `xh202615/r10_selector.py`
- Modify: `scripts/r12_dataa_rebuild_features.py`
- Test: `tests/test_r12_candidate_router.py`
- Test: `tests/test_r12_dataa_canonical.py`
- Test: `tests/test_r12_strict_holdout.py`

**Interfaces:**
- Add optional Personal VAD fields to `JoinedPvadRow` with backward-compatible zero defaults only when the feature source is absent.
- Add candidate features `asr_confidence`, `candidate_duration_ratio`, `target_speech_ratio`, and `overlap_probability` to the fixed schema.
- Preserve label-free inference and all existing selection serialization digests.

- [ ] **Step 1: Write failing tests**

Test that the router matrix contains the new finite columns, rejects NaN/Inf, preserves four actions and tie order, and that canonical/strict-holdout serialization never includes labels, reference text, or candidate CER.

- [ ] **Step 2: Run RED**

Run: `F:\XH-202615\XH-202615\.venv\Scripts\python.exe -m pytest tests\test_r12_candidate_router.py tests\test_r12_dataa_canonical.py tests\test_r12_strict_holdout.py -q`

Expected: FAIL because the new columns are not in the schema.

- [ ] **Step 3: Implement schema extension**

Thread the new fields from raw-derived feature records into `JoinedPvadRow`, use finite-zero fallback only for legacy records, and include the schema digest in the existing public router identity. Do not add validation labels to canonical rows.

- [ ] **Step 4: Run GREEN**

Run: `F:\XH-202615\XH-202615\.venv\Scripts\python.exe -m pytest tests\test_r12_candidate_router.py tests\test_r12_dataa_canonical.py tests\test_r12_strict_holdout.py -q`

Expected: PASS.

- [ ] **Step 5: Commit**

Run: `git add xh202615/r12_candidate_router.py xh202615/r12_calibrated_gate.py xh202615/r10_selector.py scripts/r12_dataa_rebuild_features.py tests/test_r12_candidate_router.py tests/test_r12_dataa_canonical.py tests/test_r12_strict_holdout.py; git commit -m "feat: route candidates with target-speaker confidence"`

### Task 5: Add focused smoke coverage and document the reproducible run

**Files:**
- Modify: `docs/r12/r12-train-and-publish.md`
- Modify: `README.md`
- Create: `tests/test_r12_overall085_contract.py`

**Interfaces:**
- The smoke contract runs without FunASR, CUDA, Dataset-A audio, labels, or model checkpoints.
- Documentation describes train/validation/internal-test boundaries and the 8GB fallback settings.

- [ ] **Step 1: Write failing tests**

Test that the README and runbook contain `Personal VAD`, `no-VAD`, `LoRA`, `8GB`, and the prohibition on internal-test labels. Test that the smoke contract imports all new modules and executes the pure feature helpers.

- [ ] **Step 2: Run RED**

Run: `F:\XH-202615\XH-202615\.venv\Scripts\python.exe -m pytest tests\test_r12_overall085_contract.py -q`

Expected: FAIL until documentation and smoke imports are added.

- [ ] **Step 3: Add documentation and smoke test**

Document the staged command sequence and explicitly state that 0.85 is a target requiring a new blind test. Keep private paths and all output names out of committed examples.

- [ ] **Step 4: Run GREEN and focused regression suite**

Run: `F:\XH-202615\XH-202615\.venv\Scripts\python.exe -m pytest tests\test_r12_overall085_contract.py tests\test_r12_asr_train.py tests\test_r12_asr_hotword.py tests\test_r12_candidate_router.py tests\test_r12_dataa_canonical.py tests\test_r12_strict_holdout.py -q`

Expected: PASS with zero failures.

- [ ] **Step 5: Commit**

Run: `git add README.md docs/r12/r12-train-and-publish.md tests/test_r12_overall085_contract.py; git commit -m "docs: describe R12 Overall 0.85 reproducible run"`

## Plan Self-Review

- Spec coverage: ASR recipe is Task 1, short-command candidates are Task 2, Personal VAD is Task 3, router/gate integration is Task 4, reproducibility and smoke coverage are Task 5.
- Data boundary: every task preserves train-only supervision and rejects internal-test paths.
- 8GB constraint: model training is sequential; LoRA and Personal VAD are bounded; unit tests do not require GPU.
- No placeholders or unspecified production interfaces remain in the task list.
- The plan does not claim an Overall result before a real grouped validation and a new blind evaluation are executed.
